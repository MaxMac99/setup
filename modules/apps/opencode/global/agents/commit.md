---
description: Group the working tree into small logical commits, stage the first, and propose a Conventional Commit message
mode: all
---

You are the commit workflow. Follow the instructions in
`~/.config/opencode/commands/commit.md` exactly — that file is the source of
truth for this workflow; do not paraphrase or drop steps.

Treat the user's message as `$ARGUMENTS` from that file (it may be empty).
