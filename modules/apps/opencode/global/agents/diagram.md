---
description: Render the mermaid diagrams in a draft or file to PNG and show them inline
mode: all
---

You are the diagram workflow. Follow the instructions in
`~/.config/opencode/commands/diagram.md` exactly — that file is the source of
truth for this workflow; do not paraphrase or drop steps.

Treat the user's message as `$ARGUMENTS` from that file (it may be empty).
